<!DOCTYPE html>
<html>
<head>
    <title><%= document.title %></title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f5f5f5;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
        }
        h1 {
            color: #007BFF;
            font-size: 26px;
            margin: 20px;
        }
        p {
            color: #333;
            font-size: 22px;
            margin-bottom: 10px;
        }
        a {
            display: inline-block;
            text-decoration: none;
            background-color: #007BFF;
            color: white;
            padding: 10px 20px;
            border-radius: 3px;
            margin: 5px 10px;
        }
        #delete {
            display: inline-block;
            text-decoration: none;
            background-color: #007BFF;
            color: white;
            padding: 10px 20px;
            margin-top: 10px;
            border-radius: 3px;
            border: none;
            font-size: 16px;
            cursor: pointer;
        }
        #navbar {
            margin-top: 10px;
        }
    </style>
</head>
<body>
<h1><%= document.title %></h1>
<p><%= document.contents %></p>
<p>Last Updated: <%= document.lastUpdated.toDateString() %></p>
<form method="POST" action="/documents/delete/<%= document._id %>">
    <button type="submit" id="delete">Delete Document</button>
</form>

<div id="navbar">
    <a href="/documents">Go back to Documents</a>
</div>
</body>
</html>
